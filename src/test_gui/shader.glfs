#version 330 core

out vec4 FragColor;

in vec4 color1;
in vec2 TexCoord;

uniform sampler2DArray texture2;
uniform int u1;

void main()
{
	float a = 1.0f + u1*0.00001f;
	//FragColor = color1;
	FragColor = a * texture (texture2, vec3 (TexCoord, u1)) + color1*0.4;
}
