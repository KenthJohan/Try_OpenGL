#version 330 core
in vec3 texpos;
out vec4 color;

uniform sampler2DArray tex;

void main()
{
	vec3 tp = texpos;
	//tp.z = 'H';
	vec4 sampled = texture (tex, tp);
	color = vec4 (1.0, sampled.r, 1.0, 1.0);
}  
