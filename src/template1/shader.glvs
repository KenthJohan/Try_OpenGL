#version 330 core

layout (location = 0) in vec4 position;
layout (location = 1) in float layer;

out vec3 texpos;



void main()
{
	gl_Position = vec4 (position.xy, 0.0f, 1.0);
	texpos = vec3 (position.zw, layer);
}

